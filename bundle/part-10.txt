';$('activeIdentifier').textContent='—';$('heroScore').textContent='0%';$('heroVerdict').textContent='Добавьте продукт или загрузите демо';
    controls.forEach(el=>el.value='');$('materialsList').innerHTML=$('suppliersList').innerHTML=$('documentsList').innerHTML='';renderMetrics(null);renderReadiness(null);renderPassportPreview(null);return;
  }
  controls.forEach(el=>{const key=el.dataset.field;if(el.type==='checkbox')el.checked=!!p[key];else el.value=p[key]??'';});
  qsa('[data-public]').forEach(el=>el.checked=p.publicVisibility?.[el.dataset.public]!==false);
  const meta=categoryMeta[p.category]||categoryMeta.other;
  $('activeName').textContent=p.name||'Без названия';$('activeCategory').textContent=meta.label;$('activeIdentifier').textContent=`${p.brand||'Без бренда'} · ${p.identifier||'без ID'} · уровень: ${levelLabel(p.passportLevel)}`;
  $('statusBadge').className=`status-badge status-${p.status||'draft'}`;$('statusBadge').textContent=statusLabel(p.status);
  renderMaterials(p);renderSuppliers(p);renderDocuments(p);renderMetrics(p);renderReadiness(p);renderPassportPreview(p);
}

function renderMetrics(p){const s=scoreProduct(p);$('completenessValue').textContent=`${s.completeness}%`;$('evidenceValue').textContent=`${s.evidence}%`;$('completenessBar').style.width=`${s.completeness}%`;$('evidenceBar').style.width=`${s.evidence}%`;$('materialsCount').textContent=p?.materials?.length||0;$('suppliersCount').textContent=p?.suppliers?.length||0;$('documentsCount').textContent=p?.documents?.length||0;$('heroScore').textContent=`${s.total}%`;$('heroVerdict').textContent=s.total>=85?'Сильная основа для отраслевой проверки':s.total>=65?'Есть база, но остаются существенные пробелы':s.total>=40?'Требуется системный сбор данных':'Паспорт пока не готов к публикации';}

function renderMaterials(p){
  $('materialsList').innerHTML=(p.materials||[]).map(m=>`<div class="dynamic-row material-row" data-row="${m.id}"><input data-key="name" value="${escapeHtml(m.name)}" placeholder="Материал"><input data-key="share" type="number" min="0" max="100" step="0.1" value="${m.share??''}"><input data-key="recycled" type="number" min="0" max="100" step="0.1" value="${m.recycled??''}"><input data-key="origin" value="${escapeHtml(m.origin)}" placeholder="Страна / регион"><select data-key="risk"><option value="low" ${m.risk==='low'?'selected':''}>Низкий</option><option value="medium" ${m.risk==='medium'?'selected':''}>Средний</option><option value="high" ${m.risk==='high'?'selected':''}>Высокий</option><option value="unknown" ${m.risk==='unknown'?'selected':''}>Неизвестно</option></select><button class="remove-row">×</button></div>`).join('')||'<div class="empty">Добавьте первый материал</div>';
  const total=(p.materials||[]).reduce((a,m)=>a+(Number(m.share)||0),0);$('materialTotal').textContent=`${Math.round(total*10)/10}%`;
  bindDynamic('materials');
}
function renderSuppliers(p){
  $('suppliersList').innerHTML=(p.suppliers||[]).map(s=>`<div class="dynamic-row supplier-row" data-row="${s.id}"><input data-key="company" value="${escapeHtml(s.company)}" placeholder="Компания"><input data-key="country" value="${escapeHtml(s.country)}" placeholder="Страна"><select data-key="tier"><option ${s.tier==='Tier 1'?'selected':''}>Tier 1</option><option ${s.tier==='Tier 2'?'selected':''}>Tier 2</option><option ${s.tier==='Tier 3'?'selected':''}>Tier 3</option></select><input data-key="component" value="${escapeHtml(s.component)}" placeholder="Компонент"><select data-key="evidence"><option value="verified" ${s.evidence==='verified'?'selected':''}>Проверено</option><option value="provided" ${s.evidence==='provided'?'selected':''}>Получено</option><option value="requested" ${s.evidence==='requested'?'selected':''}>Запрошено</option><option value="missing" ${s.evidence==='missing'?'selected':''}>Нет</option></select><button class="remove-row">×</button></div>`).join('')||'<div class="empty">Добавьте первого поставщика</div>';
  bindDynamic('suppliers');
}
function renderDocuments(p){
  $('documentsList').innerHTML=(p.documents||[]).map(d=>`<div class="dynamic-row document-row" data-row="${d.id}"><input data-key="name" value="${escapeHtml(d.name)}" placeholder="Название"><select data-key="type"><option ${d.type==='Conformity'?'selected':''}>Conformity</option><option ${d.type==='LCA / footprint'?'selected':''}>LCA / footprint</option><option ${d.type==='Supply chain'?'selected':''}>Supply chain</option><option ${d.type==='Repair / recycling'?'selected':''}>Repair / recycling</option><option ${d.type==='Safety'?'selected':''}>Safety</option><option ${d.type==='Other'?'selected':''}>Other</option></select><input data-key="reference" value="${escapeHtml(d.reference)}" placeholder="Номер или URL"><input data-key="date" type="date" value="${d.date||''}"><select data-key="verified"><option value="true" ${d.verified?'selected':''}>Да</option><option value="false" ${!d.verified?'selected':''}>Нет</option></select><button class="remove-row">×</button></div>