ct Passport')}</span><h2>${escapeHtml(p.name)}</h2><p>${escapeHtml(p.publicMessage||p.description||'Публичная информация о продукте')}</p></div><strong>${score.total}%</strong></div><div class="passport-section"><div class="passport-data-grid"><div><span>Бренд</span><b>${escapeHtml(p.brand||'—')}</b></div><div><span>Модель</span><b>${escapeHtml(p.model||'—')}</b></div><div><span>Идентификатор</span><b>${escapeHtml(p.identifier||'—')}</b></div><div><span>Уровень</span><b>${escapeHtml(levelLabel(p.passportLevel))}</b></div><div><span>Производитель</span><b>${escapeHtml(p.manufacturer||'—')}</b></div><div><span>Страна</span><b>${escapeHtml(p.manufactureCountry||'—')}</b></div></div></div>${p.publicVisibility?.materials!==false?`<div class="passport-section"><h3>Основные материалы</h3>${mats.length?mats.map(m=>`<span class="passport-pill">${escapeHtml(m.name)} · ${Number(m.share)||0}%</span>`).join(''):'<p>Нет опубликованных данных</p>'}</div>`:''}${p.publicVisibility?.environment!==false?`<div class="passport-section"><h3>Цикличность и срок службы</h3><div class="passport-data-grid"><div><span>Ремонтопригодность</span><b>${p.repairScore!==''?escapeHtml(p.repairScore)+'/10':'—'}</b></div><div><span>Перерабатываемость</span><b>${p.recyclablePct!==''?escapeHtml(p.recyclablePct)+'%':'—'}</b></div><div><span>Гарантия</span><b>${p.warrantyMonths!==''?escapeHtml(p.warrantyMonths)+' мес.':'—'}</b></div><div><span>Срок службы</span><b>${p.expectedLifeYears!==''?escapeHtml(p.expectedLifeYears)+' лет':'—'}</b></div></div></div>`:''}<div class="passport-section"><small>${escapeHtml(branding.footerText)}</small></div>`;
  const url=qrPublicUrl(p);$('qrImage').src=`https://api.qrserver.com/v1/create-qr-code/?size=220x220&margin=8&data=${encodeURIComponent(url)}`;
}

function showPublicMode(payload){
  const p=payload.product,b=payload.branding||branding,s=p.score||{total:0,evidence:0};document.documentElement.style.setProperty('--accent',b.accentColor||'#7cf7a5');$('appShell').classList.add('hidden');$('publicMode').classList.remove('hidden');
  const materials=(p.materials||[]).map(m=>`<tr><td>${escapeHtml(m.name)}</td><td>${Number(m.share)||0}%</td><td>${Number(m.recycled)||0}%</td><td>${escapeHtml(m.origin||'—')}</td></tr>`).join('');
  const suppliers=(p.suppliers||[]).map(x=>`<tr><td>${escapeHtml(x.company)}</td><td>${escapeHtml(x.country)}</td><td>${escapeHtml(x.component)}</td><td>${escapeHtml(x.evidence==='verified'?'Проверено':x.evidence==='provided'?'Получено':'В процессе')}</td></tr>`).join('');
  const docs=(p.documents||[]).map(d=>`<tr><td>${escapeHtml(d.name)}</td><td>${escapeHtml(d.type)}</td><td>${escapeHtml(d.reference)}</td><td>${escapeHtml(d.date||'—')}</td></tr>`).join('');
  $('publicMode').innerHTML=`<button class="public-back" id="publicBack">← Рабочая область</button><div class="public-container"><header class="public-header"><div><span class="eyebrow">${escapeHtml(p.publicTitle||'Digital Product Passport')}</span><h1>${escapeHtml(p.name)}</h1><p>${escapeHtml(p.publicMessage||p.description||'')}</p></div><div class="public-score"><span>Data readiness</span><strong>${Number(s.total)||0}%</strong><small>Подтверждено: ${Number(s.evidence)||0}%</small></div></header><main class="public-body"><section class="public-section"><div class="public-grid"><div><small>Бренд</small><h3>${escapeHtml(p.brand||'—')}</h3></div><div><small>Модель</small><h3>${escapeHtml(p.model||'—')}</h3></div><div><small>Идентификатор</small><h3>${escapeHtml(p.identifier||'—')}</h3></div><div><small>Уровень паспорта</small><h3>${escapeHtml(levelLabel(p.passportLevel))}</h3></div><div><small>Производитель</small><h3>${escapeHtml(p.manufacturer||'—')}</h3></div><div><small>Экономический оператор</small><h3>${escapeHtml(p.economicOperator||'—')}</h3></div></div></section><section class="public-section"><h2>Описание и назначение</h2><p>${escapeHtml(p.description||'—')}</p><p>${escapeHtml(p.intendedUse||'')}</p></section>${materials?`<section class="public-section"><h2>Материалы</h2><table class="public-table"><thead><tr><th>Материал</th><th>Доля</th><th>Переработанное</th><th>Происхождение</th></tr></thead><tbody>${materials}</tbody></table></section>`:''}${suppliers?`<section class="public-section"><h2>Цепочка поставок</h2><table class="public-table"><thead><tr><th>Компания</th><th>Страна</th><th>Компонент</th><th>Статус</th></tr></thead><tbody>${suppliers}</tbody></table></section>`:''}${p.environment?`<section class="public-section"><h2>Экологические и эксплуатационные данные</h2><div class="public-grid"><div><small>Углеродный след</small><h3>${valUnit(p.environment.carbonFootprint,'кг CO₂e')}</h3></div><div><small>Ремонтопригодность</small><h3>${valUnit(p.environment.repairScore,'/10')}</h3></div><div><small>Перерабатываемая доля</small><h3>${valUnit(p.environment.recyclablePct,'%')}</h3></div><div><small>Срок службы</small><h3>${valUnit(p.environment.expectedLifeYears,'лет')}</h3></div><div><small>Гарантия</small><h3>${valUnit(p.envir